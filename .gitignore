# donnees et checkpoints : trop lourds pour git — voir la release pour les poids,
# et `python run.py prepare` pour regenerer les corpus
data/
data-v2/
# v4 : garder seulement les petits artefacts reproductibles explicitement suivis
# (tokenizer, meta et pools RLAIF), jamais les corpus bruts locaux.
data-v4/raw/
runs/
__pycache__/
*.pt
*.bin
*.mask
.cache/
tmp/
.env
.env.*
!.env.example
*.pem
